# Digesto AI - Environment Variables
# Copia questo file come .env.local per sviluppo locale

# URL pubblico dell'endpoint chat del backend
# Lascia vuoto per attivare la modalità mock/demo
# Oppure imposta: https://api.tuodominio.tld oppure /api/chat (per usare il proxy)
NEXT_PUBLIC_API_URL=

# URL privato del backend (usato solo dal proxy server-side /api/chat)
# Non viene mai esposto al client
# BACKEND_URL=https://api.tuodominio.tld
